services:
  backend:
    image: ${YLOADER_BACKEND_IMAGE:-yloader-backend}
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: yloader-backend
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      - DOWNLOAD_DIR=/downloads
      - DB_PATH=/app/data/metadata.db
      - FFMPEG_PATH=/usr/local/bin/ffmpeg
      - FFPROBE_PATH=/usr/local/bin/ffprobe
      - YT_DLP_MANAGED_BY_YLOADER=1
      - FFMPEG_MANAGED_BY_YLOADER=1
    volumes:
      - ./downloads:/downloads
      - ./backend-data:/app/data
  frontend:
    image: ${YLOADER_FRONTEND_IMAGE:-yloader-frontend}
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: yloader-web
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
